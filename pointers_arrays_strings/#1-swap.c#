#include "main.h"
#include <stdio.h>

/**
 * swap_int - swaps the values of two integers
 *using two input parameters
 *
 *@a: input parameter 1
 *@b: imput parameter 2
 *
 *Return: Nothing
 */

void swap_int(int *a, int *b)
{
  int temp;

  temp = *a;
  *a = 98;
  *b = 42;
  printf("a=98%d, b=42%d\n" , a, b)
    swap_int (&a, &b)
    return (0)
    }