#include "main.h"
#include <string.h>

/**
* print_rev -> printing a string in reverse
*@: the string to be printed in rev
*/
void print_rev (char *s)
{
int i, n;
s = "hello"
n = strlens (s);
_put char(s[i]);
}
